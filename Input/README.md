# Input フォルダ運用

担当者ごと・プロジェクトごとに工程表Excel（.xlsx）を配置してください。

例:
- `Input/Shiraishi/NRT82/NRT82_schedule.xlsx`
- `Input/Tanaka/HND10/HND10_schedule.xlsx`

注意:
- Excel内部に `Project Name` や `Project Manager` がある場合はExcel値を優先します。
- フォルダ名との矛盾は自動修正せず Validation Error として記録します。
